#include "main.h"

/**
 * _islower - checks for lowercase character
 *@c: single letter input
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	int r;

	if (c > 97)
		return (1);
	else
		return (0);
}

